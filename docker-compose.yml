services:
  api:
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - 8080:8080
    environment:
      - PORT=8080
    volumes:
      - .:/app
    working_dir: /app
    command: node index.js
    restart: unless-stopped